{% extends 'base.html.twig' %}

{% block body %}

	{{ comment.commentAuthor.name}} {{ comment.commentAuthor.lastName }} dodał nowy komentarz: <br>
	{{ comment.content }}<br>
	<br>
	<a href="{{ url('comment_form', {projectId: projectId, authorId: member}) }}">
		<button class="btn btn-warning"> Odpowiedz</button>
	</a>
{% endblock %}